# ☕ /coffee cycle — mobile — 2026-09-03 05:13 (autonomous cron dbf9c5fa)

Focus: **mobile design.** Bar: mobile AS READABLE AND USABLE AS DESKTOP. Winning = ship the top
low-risk item that most closes the mobile-vs-desktop readability gap, verified at 375px.

## Phase 2 — RESEARCH (reused)
Reused tonight's fresh mobile research brief (workflow wvs2svhh7, 2026-09-02-mobile.md) — 16px min
body/inputs, ≥14px secondary, price prominent, ≥44px tap targets, no 320px overflow, monochrome
hierarchy by size/weight/case/rules. No re-run (budget guardrail; also avoids stacking a fan-out
with the live scan loop).

## Phase 3 — COMPARE (gap)
Highest-leverage on-the-bar item = the audit's **worst-scoring area (shell-nav 6.0)**: the phone
store rail (sidebar.css:190-193) hid `.rail-name` and showed the 2-letter `.rail-badge` code, so a
375px phone showed "HD / TG / LW / WM" where desktop shows full names — VERIFIED live (labels came
back ["HD","TG",...]). That is strictly LESS readable than desktop = a direct bar violation. Chips
were also ~40px tall (padding --s3), under the 44px tap-target floor.

## Phase 5 — BLUEPRINT
CSS-only, in-constitution, one element (the mobile rail): show `.rail-name`, hide `.rail-badge`,
hide the generic `.rail-icon` (reclaims width + honors no-generic-icons on the crowded phone rail),
and set `.rail-item { min-height:44px }`. Chips size to content in the existing horizontal scroller.
Risk: LOW. Verify: re-measure at 375px (names shown, badge/icon hidden, chip ≥44px, no overflow).

## Phase 6 — EXECUTE
- Edited web/src/sidebar.css (the ≤760px block). Commit f9898ef.
- Typecheck: `npm run web:build` ✅ (CSS-only; tsc unaffected). Deploy: push + Railway redeploy → {"success":true}.

## Phase 7 — CONCLUDE — ✅ SHIPPED & VERIFIED
New bundle `index-Crcu2_AS.css` live. Re-measured at 375px:
- rail labels now `["HOME DEPOT","TARGET","LOWE'S","WALMART","BEST BUY","NEWEGG"]` (was `["HD","TG",...]`) ✅
- nameShown true, badgeShown false, iconShown false ✅
- chip height 45px (≥44px tap target) ✅
- no horizontal body overflow (rail scrolls internally) ✅
Screenshot confirms full names, receipt-styled (mono uppercase, hard borders, zero radius).
Closes the Phase-3 gap, hits the bar (mobile rail now as readable as desktop). No regression → no revert.

## Phase 8 — TEST
Metric re-measured live (labels + tap target + overflow) → passed. Fuller /roleplay mobile journey = follow-up.

## Next mobile items (backlog)
- Deal-card image tall (~457px card) → right-hand thumbnail / shorter image for more deals per screen. MEDIUM.
- ZIP input placeholder clips ("ZIP CO…") at 375px. LOW quick win.
- Consolidate the two duplicate ≤760px @media blocks in sidebar.css (polish).
- Long tail of 13 name chips = long horizontal scroll; consider a "More" disclosure. MEDIUM.

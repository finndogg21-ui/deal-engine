# ☕ /coffee cycle — mobile — 2026-09-03 ~12:13 (cron dbf9c5fa)

Focus: mobile design. Bar: mobile AS READABLE/USABLE AS DESKTOP + shows more than one deal per screen.

## Phase 2 — RESEARCH (reused)
Reused the mobile brief (workflow wvs2svhh7 / 2026-09-02-mobile.md) — no re-fan-out (budget + no
stacking with the scan loop, which was on a heartbeat).

## Phase 3 — COMPARE (measured live at 375px — the real numbers)
- **Chrome above the feed = 571px** (70% of the 812 viewport): `.tape-head` 194 + `.rail` 70 +
  `.spools`/toolbar 234. → **0 cards fully in view on load.**
- **Deal card = 458px** tall (2-up, 172px wide). Image is only **25%** (117px); the body (342px)
  is the bulk: card-price **71px**, card-facts **60px**, card-off **54px**, title 36, retailer 18.
- **ZIP-placeholder clip = FALSE positive** — measured `zipClips: false` ("ZIP code" fits the 88px
  field; the earlier screenshot read was wrong). Not shipping a fix for a non-issue.

Honest conclusion: the two real density levers are both MEDIUM-risk (blueprint, not auto-ship):
(1) the 571px chrome, (2) the card CONTENT (71px price / 60px facts). The clean LOW-risk win is
tightening the card's wasted vertical spacing.

## Phase 5 — BLUEPRINT
SHIP (low-risk): tighten mobile card vertical rhythm — `.deck .card-body { gap: --s1 }` (from --s2),
`.deck .card-price { min-height: 0 }` (drop the six-up 46px reservation), `.deck .card-off` margins
trimmed. CSS-only, no content/fonts changed.

DEFER → owner (MEDIUM-risk, higher-leverage — the real density wins):
- **Chrome reduction** (the 571px): collapse-on-scroll header, or move the ZIP bar / compact the
  toolbar (spools 234px) on mobile. Touches the shell layout → medium risk.
- **Card content density**: the 71px price block (over-tall — likely an "as low as" label + wrap +
  min-height stacking) and the 60px facts block. Trimming/clamping risks honesty copy → medium risk.
- **Right-hand thumbnail** card layout (research-preferred for a decision-by-numbers feed) — bigger
  restructure.

## Phase 6 — EXECUTE
Edited web/src/dashboard.css mobile block (+7 lines). Commit 58f98c7. web:build ✅. Deploy ✅.

## Phase 7 — CONCLUDE — ✅ SHIPPED & VERIFIED (modest)
Bundle index-DVPc9j5Q.css live. Re-measured at 375px: card **458px → 433px** (−25px, body 342→314),
still 2-across, no overflow. Real but modest (~5.5%). Notably `card-price` stayed **71px** even after
`min-height:0` → the price block is genuinely CONTENT-tall, not a wasted reservation. That confirms
the remaining density gains are in the content (medium-risk), not spacing. No regression → no revert.

## Phase 8 — TEST
Re-measured card height live (458→433) + confirmed 2-across + no overflow. Passed.

## HONEST CYCLE VERDICT
Mobile is now at the low-risk floor: 4 shipped improvements (2-col grid, CTA-hide, rail names, this
spacing tighten). Further density is blocked by MEDIUM-risk levers that need owner sign-off:
1. **Chrome reduction** (571px above the feed) — the single biggest lever. Collapse-on-scroll header.
2. **Card content** (71px price / 60px facts) — trim/clamp, but touches honesty copy.
Recommend the next mobile cycle be owner-directed on ONE of these, not another marginal CSS pass.

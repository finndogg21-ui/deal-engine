node_modules/
dist/
.env
.env.local

# PGlite local database directory — throwaway dev data, never committed
.data/

# Raw vendor payloads captured for replay tests
fixtures/raw/*.json
!fixtures/raw/.gitkeep

*.log
.DS_Store
Thumbs.db

# Proxy credentials. Never committed.
proxies.txt
proxies*.txt

# Any .env variant (backups, .local, .production) is secrets.
.env.*
!.env.example

# Corrupted-DB backups (any .data variant is database, never committed)
.data*/
logs/

# Local prod data dumps (regenerate with scripts/dump-prod-hd.mts)
hd-prod.json

# scan output dumps
scripts/out/
experiments/*/node_modules/
experiments/*/out/
